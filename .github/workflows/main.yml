# SSH/SFTP Deploy

name: SSH Deploy

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: SSH deployment every commit
    runs-on: ubuntu-latest
    steps:
    - name: Get latest code
      uses: actions/checkout@v4

    - name: Install packages
      uses: actions/setup-node@v3
      with:
        node-version: 18
        #cache: "npm"
    #- run: npm ci
    - name: Install npm dependencies
      run: npm install

    - name: Build project
      run: npm run build --if-present
      env:
        CI: false

    - name: Deploy to server
      uses: easingthemes/ssh-deploy@main
      with:
        SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIV_KEY }}
        ARGS: "-rlgoDzvc -i --delete"
        SOURCE: "dist/"
        REMOTE_HOST: ${{ secrets.SFTP_SERVER }}
        REMOTE_USER: ${{ secrets.SFTP_USER }}
        TARGET: ${{ secrets.REMOTE_TARGET }}
        SCRIPT_BEFORE: |
          whoami
          ls -al

    #- name: Setup SSH
      #run: |
        #mkdir -p ~/.ssh/
        #echo "${{ secrets.SSH_PRIV_KEY }}" > ~/.ssh/id_rsa
        #echo "${{ secrets.SSH_PUB_KEY }}" > ~/.ssh/id_rsa.pub
        #chmod 600 ~/.ssh/id_rsa
        #chmod 600 ~/.ssh/id_rsa.pub
        #ssh-keyscan -H ${{ secrets.SFTP_SERVER }} >> ~/.ssh/known_hosts
        
    #- name: Transfer / Copy builds to server
      #run: |
        #rsync -vrm .output/* ${{ secrets.SFTP_USER }}:/home/cwbx9ait2_radiozyh/test/
        #ssh ${{ secrets.SFTP_USER }} "pm2 restart ecosystem.config.js"

    #- name: Sync Files
      #uses: SamKirkland/web-deploy@v1
      #uses: wlixcc/SFTP-Deploy-Action@v1.2.4
      #with:
        #wlixcc:
        #username: ${{ secrets.SFTP_USER }}
        #server: ${{ secrets.SFTP_SERVER }}
        #password: ${{ secrets.SFTP_PASS }}
        #ssh_private_key: ${{ secrets.SSH_PRIV_KEY }}
        #local_path: ./*
        #remote_path: ${{ secrets.REMOTE_TARGET }}
        #sftpArgs: '-o ConnectTimeout=5'

        #kirkland:
        #target-server: ${{ secrets.SFTP_SERVER }}
        #remote-user: ${{ secrets.SFTP_USER }}
        #private-ssh-key: ${{ secrets.SSH_PRIV_KEY }}
        #destination-path: ${{ secrets.REMOTE_TARGET }}


# FTP Deploy

#on:
#  push:
#    branches:
#      - main
#name: Deploy
#jobs:
#  web-deploy:
#    name: FTP-Deploy
#    runs-on: ubuntu-latest
#    steps:
#    - name: Get latest code
#      uses: actions/checkout@v3
#    
#    - name: Sync Files
#      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
#      with:
#        server: ${{ secrets.SFTP_SERVER }}
#        username: ${{ secrets.SFTP_USER }}
#        password: ${{ secrets.SFTP_PASS }}
#        server-dir: /cwbx9ait2_radiozyh/webroots/r1033653/public_html/
